# 🚀 Professional 3D DUI

Einfaches System zum Rendern von HTML-Inhalten in FiveM.

### Installation
1. Ordner in `resources` schieben.
2. `ensure DUI` in die CFG.

### Editor
Nutze `/placedui`, schiebe den Screen mit den Pfeiltasten und speichere mit `Enter`.